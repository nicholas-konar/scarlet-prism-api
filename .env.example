# ---------- App ----------
PORT=3000
OPENAI_API_KEY=changeme

# ---------- Postgres ----------
POSTGRES_USER=username
POSTGRES_PASSWORD=password
POSTGRES_DB=scarletprism
# For Prisma locally (host -> container mapping)
DATABASE_URL=postgresql://scarlet:password@localhost:5432/scarletprism?schema=public

# ---------- Redis ----------
REDIS_URL=redis://localhost:6379

# ---------- Mail ----------
SMTP_HOST=localhost
SMTP_PORT=1025
